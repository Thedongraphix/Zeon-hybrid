import { handleApiRequest } from '../index.js';

// This function is the entry point for a serverless environment.
// It simply passes the request and response objects to the main handler.
export default handleApiRequest; 